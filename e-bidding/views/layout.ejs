<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>E-Bidding System</title>
</head>
<body>
    <header>
        <h1>E-Bidding System</h1>
        <nav style="display: flex; justify-content: space-between; align-items: center;">
            <div>
                <a href="/">Home</a>
                <a href="/items/list">Listings</a>
                <% if (user) { %>
                    <% if (user.role === 'S') { %>
                        <a href="/superuser/superuser_dashboard">Admin Dashboard</a>
                    <% } else { %>
                        <a href="/items/add">Add Listing</a>
                        <a href="/user/dashboard">Dashboard</a>
                        <a href="/support">Support</a>
                    <% } %>
                <% } else { %>
                    <a href="/auth/login">Login</a>
                    <a href="/auth/register">Register</a>
                <% } %>
            </div>
            <% if (user) { %>
                <div>
                    <form action="/auth/logout" method="GET" style="display: inline;">
                        <button type="submit" style="background: none; border: none; cursor: pointer; font-size: 1rem;">
                            Logout
                        </button>
                    </form>
                </div>
            <% } %>
        </nav>
    </header>
    <main>
        <%- body %>
    </main>
</body>
</html>